<% layout('/layouts/boilerplate') %>

            <h1>All Projects</h1>
            <ul>
                <% for (let boardName of allBoardsName) { %>
                    <li>
                        <%= boardName %>
                    </li>
                    <% } %>
            </ul>

